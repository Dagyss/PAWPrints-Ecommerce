apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: paw-print
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: php
          image: gcr.io/matchmaking-app-paw/php-app:20250606-paw-print-v2
          ports:
            - containerPort: 9000
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: DB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_ROOT_PASSWORD
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
        - name: nginx
          image: gcr.io/matchmaking-app-paw/nginx-app:latest
          ports:
            - containerPort: 80
